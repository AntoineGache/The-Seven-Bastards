import {NextFunction, Request, Response} from 'express';
import { MySQL } from "../../core/database/MySQL";

export const addCompetiteur = async (req: Request, res: Response, next: NextFunction) => {


}